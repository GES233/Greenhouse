defmodule Greenhouse.Steps.ContentReplacer do

  # @replaced_pattern ~r/:{.*}/

  # Inner Recipes

  def replace_posts([_posts_map, _media_map], _opts) do
    # ...
  end

  def replace_pages([_pages_map, _media_map], _opts) do
    # ...
  end
end
